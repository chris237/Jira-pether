<?xml version="1.0" encoding="UTF-8"?>

<odoo>
    <data>
        <!-- <record id="account_journal_dashboard_kanban_view_inherit" model="ir.ui.view">
            <field name="name">account.journal.dashboard.kanban.inherited</field>
            <field name="model">account.journal</field>
            <field name="inherit_id" ref="account.account_journal_dashboard_kanban_view" />
            <field name="arch" type="xml">
                <xpath expr="//t[@t-name='JournalBodyBankCash']//div[hasclass('o_kanban_primary_right')]" position="inside">
                    <div t-if="journal_type == 'bank' and dashboard.num_checks_to_print != 0">
                        <div class="row">
                            <div class="col-12">
                                <a type="object" name="action_checks_to_print">
                                    <t t-esc="dashboard.num_checks_to_print"/>
                                    <t t-if="dashboard.num_checks_to_print == 1">Cheque to print</t>
                                    <t t-if="dashboard.num_checks_to_print != 1">Cheques to print</t>
                                </a>
                            </div>
                        </div>
                    </div>
                </xpath>
            </field>
        </record> -->
        <!-- <record id="view_pay_tax" model="ir.ui.view">
            <field name="name">account.payment.form</field>
            <field name="model">account.payment</field>
            <field name="inherit_id" ref="account.view_account_payment_form"/>
            <field name="arch" type="xml">
                <xpath expr="//div[@name='amount_div']" position=" before">
                    <div name="discout_div" class="o_row">
                        <field name="disc"/>
                        <field name="currency_id" options="{'no_create': True, 'no_open': True}" groups="base.group_multi_currency"/>
                    </div>
                    <div name="claims_div" class="o_row">
                        <field name="tclaims"/>
                        <field name="currency_id" options="{'no_create': True, 'no_open': True}" groups="base.group_multi_currency"/>
                    </div>
                </xpath>
            </field>
        </record>  -->
        <!-- <record id="invoice_import_SUS" model="ir.ui.view">
            <field name="name">partner.view.buttons</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form" />
            <field name="priority" eval="11"/>
            <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]"/>
            <field name="arch" type="xml">
                <div name="button_box" position="inside">
                    <button class="oe_stat_button" type="object" name="get_suscribers"
                        icon="fa-file-text" >
                        <div class="o_form_field o_stat_info">
                            <span class="o_stat_text">Invoice sus</span>
                        </div>
                    </button>
                </div>
            </field>
        </record>		
        <record id="invoice_import_SP" model="ir.ui.view">
            <field name="name">partner.view.buttons</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form" />
            <field name="priority" eval="11"/>
            <field name="groups_id" eval="[(4, ref('account.group_account_invoice'))]"/>
            <field name="arch" type="xml">
                <div name="button_box" position="inside">
                    <button class="oe_stat_button" type="object" name="get_sp"
                        icon="fa-file-text" >
                        <div class="o_form_field o_stat_info">
                            <span class="o_stat_text">Invoice SP</span>
                        </div>
                    </button>
                </div>
            </field>
        </record> -->
    </data>
</odoo>